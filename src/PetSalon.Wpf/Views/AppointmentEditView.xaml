<UserControl x:Class="PetSalon.Wpf.Views.AppointmentEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:PetSalon.Wpf.ViewModels"
             xmlns:helper="clr-namespace:PetSalon.Wpf.Behaviors"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance vm:AppointmentEditViewModel}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" /><RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="預約資料" FontWeight="Bold" FontSize="16" Margin="0,0,0,12"
                       Foreground="{StaticResource Brown700Brush}" />

            <TextBlock helper:LabelHelper.RequiredText="飼主" Style="{StaticResource FieldLabelStyle}" />
            <ComboBox ItemsSource="{Binding AvailableOwners}"
                      DisplayMemberPath="Name"
                      SelectedValuePath="OwnerId"
                      SelectedValue="{Binding SelectedOwnerId}" />

            <TextBlock helper:LabelHelper.RequiredText="寵物" Style="{StaticResource FieldLabelStyle}" Margin="0,10,0,4" />
            <ComboBox ItemsSource="{Binding AvailablePets}"
                      DisplayMemberPath="Name"
                      SelectedValuePath="PetId"
                      SelectedValue="{Binding PetId}" />

            <TextBlock helper:LabelHelper.RequiredText="日期" Style="{StaticResource FieldLabelStyle}" Margin="0,10,0,4" />
            <DatePicker SelectedDate="{Binding Date, Converter={StaticResource DateOnlyToDateTimeConverter}}" />

            <TextBlock helper:LabelHelper.RequiredText="時間 (HH:mm)" Style="{StaticResource FieldLabelStyle}" Margin="0,10,0,4" />
            <TextBox Text="{Binding TimeText, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Text="備註" Style="{StaticResource FieldLabelStyle}" Margin="0,10,0,4" />
            <TextBox Text="{Binding Note, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" MinHeight="80" TextWrapping="Wrap" />

            <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}" Margin="0,10,0,0"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource NotNullToVisibilityConverter}}" />
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="取消" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding CancelCommand}" Margin="0,0,8,0" />
            <Button Content="儲存" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding SaveCommand}" IsDefault="True" />
        </StackPanel>
    </Grid>
</UserControl>
