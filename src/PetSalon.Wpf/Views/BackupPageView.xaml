<UserControl x:Class="PetSalon.Wpf.Views.BackupPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:PetSalon.Wpf.ViewModels"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance vm:BackupPageViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /><RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
            <DockPanel>
                <Button DockPanel.Dock="Right" Style="{StaticResource PrimaryButtonStyle}"
                        AutomationProperties.AutomationId="btn-create-backup"
                        Content="💾 建立備份" Command="{Binding CreateBackupCommand}" />
                <StackPanel>
                    <TextBlock Text="備份管理" FontWeight="Bold" FontSize="20" Foreground="{StaticResource Brown700Brush}" />
                    <TextBlock Text="一鍵備份所有資料與契約 PDF，存成 .zip 檔案。"
                               Foreground="{StaticResource Brown500Brush}" Margin="0,4,0,0" />
                </StackPanel>
            </DockPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="0">
            <DockPanel>
                <Border DockPanel.Dock="Top" Padding="16,12" Background="{StaticResource Pink50Brush}"
                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
                    <DockPanel>
                        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                            <Button Style="{StaticResource SecondaryButtonStyle}" AutomationProperties.AutomationId="btn-refresh-backup" Content="重新整理" Command="{Binding RefreshCommand}" />
                            <Button Style="{StaticResource SecondaryButtonStyle}" AutomationProperties.AutomationId="btn-restore-backup" Content="還原" Command="{Binding RestoreCommand}" Margin="8,0,0,0" />
                            <Button Style="{StaticResource DangerButtonStyle}" AutomationProperties.AutomationId="btn-delete-backup" Content="刪除" Command="{Binding DeleteBackupCommand}" Margin="8,0,0,0" />
                        </StackPanel>
                        <TextBlock Text="現有備份" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="{StaticResource Brown700Brush}" />
                    </DockPanel>
                </Border>
                <DataGrid AutomationProperties.AutomationId="grid-backups" ItemsSource="{Binding Backups}" SelectedItem="{Binding Selected}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="檔名" Binding="{Binding FileName}" Width="2*" />
                        <DataGridTextColumn Header="建立時間" Binding="{Binding CreatedAt, StringFormat='yyyy/MM/dd HH:mm:ss'}" Width="*" />
                        <DataGridTextColumn Header="大小 (bytes)" Binding="{Binding SizeBytes, StringFormat=N0}" Width="120" />
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
