<UserControl x:Class="PetSalon.Wpf.Controls.SignaturePad"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="400" d:DesignHeight="160">
    <Border BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1" CornerRadius="4" Background="White">
        <Grid>
            <InkCanvas x:Name="Ink" Background="Transparent" />
            <Button Style="{StaticResource SecondaryButtonStyle}"
                    Content="清除"
                    Click="OnClear"
                    HorizontalAlignment="Right" VerticalAlignment="Bottom"
                    Margin="0,0,8,8" Padding="10,4" FontSize="11" />
            <TextBlock x:Name="Placeholder"
                       Text="請在此簽名"
                       Foreground="{StaticResource MutedBrush}"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       IsHitTestVisible="False" />
        </Grid>
    </Border>
</UserControl>
